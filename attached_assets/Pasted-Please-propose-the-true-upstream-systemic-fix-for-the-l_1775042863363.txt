Please propose the true upstream/systemic fix for the latest BirthdayGreetingsV13 package-generation failures.

I do NOT want another symptom-level patch list.
I do NOT want a narrow quality-gate-only answer.
I want the real next task or task set that addresses the highest-leverage upstream/systemic causes of the latest run.

Context
Latest evidence:
- BirthdayGreetingsV13 completed with major errors
- DHG readiness is 19%
- 3 workflows are not Studio-loadable
- 4 workflows are stubs/openable-with-warnings but not truly generated
- unresolved activity/type issues remain (ShouldRetry, GetAsset, Switch, GoogleContactsSearchContacts, UpdateEntity, IntegrationServiceHTTPRequest, RetryScope member issues)
- EndProcess has a HANDOFF_STRING_FORMAT_UNSAFE / String.Format-related cascade
- many undeclared-variable findings appear to be inflated by sentinel/handoff expression handling
- Main and ProcessTransaction were marked structurally invalid partly because complex expressions emitted “as-is” were still treated as Studio-blocking
- the package widened to a richer V13 architecture and more activity families, but artifact truth is still not strong enough

What I already believe
There are at least three different failure clusters here:

1. Expression / sentinel / quality-gate classification failures
Examples:
- HANDOFF_STRING_FORMAT_UNSAFE poisoning undeclared-variable checks
- COMPLEX_EXPRESSION_PASSTHROUGH being treated like structurally broken syntax
- false-positive undeclared-variable cascades in EndProcess.xaml

2. Final artifact dependency / namespace / type truth failures
Examples:
- unresolved ShouldRetry
- unresolved GetAsset
- unresolved Switch
- unresolved GoogleContactsSearchContacts / UpdateEntity / IntegrationServiceHTTPRequest
- RetryScope member/Implementation shape problems
These are not just “quality gate” issues — Studio itself cannot resolve the emitted artifact correctly.

3. Over-broad emitted implementation surface
Examples:
- richer architecture and more activity families were emitted before the system had fully proven those families and patterns are safe
- V13 widened to Dispatcher / Performer / ProcessTransaction / EndProcess / InitializeConfig / GenerateAndValidateGreeting etc.
- more packages and activity families were pulled in than the artifact truth layer can currently support safely

What I do NOT want
Do NOT give me:
- broad scanner-based undeclared-variable auto-declaration
- default unknown identifiers to x:String
- more broad regex expression salvage
- “just improve the prompt”
- a generic “fix quality gate” answer that ignores real Studio resolution failures
- a broad rollback of all recent work unless truly necessary

What I want from you
I want you to propose the true next upstream/systemic fix, grounded in the latest V13 evidence.

Please answer these questions directly:
1. What is the highest-leverage upstream/systemic fix now?
2. Is the correct next move:
   - a bounded expression/sentinel contract fix,
   - a final artifact type/header/dependency truth fix,
   - an emission-surface narrowing fix,
   - or a combined task set?
3. Which failures from the V13 run are mostly noise inflation vs which are real artifact-truth failures?
4. If you had to choose only 1-2 tasks next, what would they be and why?
5. What should explicitly stay out of scope to avoid going in circles again?

Important
I want you to distinguish clearly between:
- fixes that improve DHG/quality-gate truthfulness
and
- fixes that actually make the Studio artifact more openable/resolvable

I suspect the system may now need to enforce a stronger contract like:
- “only emission-approved workflow patterns + emission-approved activity families + emission-approved expression shapes may reach final packaging”
Please assess whether that is the right framing.

Output format
1. Short verdict
2. Root-cause hierarchy
3. Proposed next task or task set
Use:
- title
- what & why
- done looks like
- out of scope
- tasks
- relevant files
4. Why this is upstream/systemic instead of another symptom patch

Be honest
If the right answer is “the system widened the emission surface before proving artifact truth,” say that plainly.
If the right answer is “fixes A/B are helpful but not the real next lever,” say that plainly.
