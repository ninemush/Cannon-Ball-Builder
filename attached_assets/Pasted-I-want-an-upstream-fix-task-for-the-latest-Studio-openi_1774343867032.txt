I want an upstream fix task for the latest Studio-opening failure in the generated UiPath package.

Important:
- Do NOT patch this package manually
- Do NOT treat this as a one-off cleanup
- This must fix the upstream generator/remediation logic
- The goal is for generated packages to be Studio-openable and compile-safe by construction

Context
The latest generated package still fails in UiPath Studio with:

- `Set property 'System.Activities.InArgument(System.Object).Expression' threw an exception`

Current root-cause analysis indicates this is caused by multiply-nested `<InArgument>` / `<OutArgument>` wrappers generated by two separate upstream passes:

1. `compliancePass` in `xaml-compliance.ts`
- wraps `<Assign.To>` / `<Assign.Value>` children in `OutArgument` / `InArgument`

2. quality-gate auto-remediation in `package-assembler.ts`
- re-wraps properties that appear to need argument wrappers
- its current regex/guard is not actually idempotent
- it matches whitespace/newlines before an already-present `<InArgument>` / `<OutArgument>`
- so it wraps the same property again

The current de-duplication logic only handles shallow duplication and cannot recover from deep nesting once both passes stack repeatedly.

Additional issue confirmed by Studio:
- package dependency pinning is still not trustworthy, e.g. `UiPath.System.Activities 25.10.7` not found in the current feeds / validated environment

Secondary issue:
- variable typing is still too weak (`x:Object` everywhere), but this is lower priority than the compile/open failures

What I want
Create upstream tasks/fixes that address the real causes of these failures.

Please return one or two linked tasks, depending on whether you think they should be split, covering at minimum:

1. Assign argument-wrapper idempotence
- there must be one authoritative wrapping behavior for `Assign.To` / `Assign.Value`
- if multiple passes remain, they must be truly idempotent
- no already-wrapped property should ever be wrapped again
- whitespace/newlines must not fool the wrapper detection logic
- nested wrappers must not survive to final output

2. Post-pass validation for argument structure
- after all generation/remediation passes, validate that `Assign.To` and `Assign.Value` contain exactly one correct argument wrapper layer
- if not, normalize known-safe cases or fail generation explicitly
- do not allow deeply nested wrappers to ship

3. Dependency version authority
- fix package dependency version resolution so emitted versions are actually valid/restorable in the target environment / validated registry
- do not emit versions that are not in the approved source of truth
- `UiPath.System.Activities` versioning must no longer drift into non-restorable values

4. Studio-open / compile safety
- final generated package should open in Studio without the current `InArgument(...).Expression` exception
- dependency restore should not fail on invalid pinned versions

5. Keep lower-priority typing debt separate if needed
- if variable typing improvements are worth doing, classify them separately as quality follow-up, not as the main blocker fix

What I want back
Please return:
1. the proposed task(s) in the usual format:
- title / objective
- root-cause summary
- impact analysis
- done looks like
- out of scope
- tasks
- relevant files
- acceptance criteria
- dependencies

2. clear recommendation on whether this should be:
- one combined upstream correctness task
- or two linked tasks:
  - XAML argument-wrapper correctness
  - dependency version authority

Important constraints
- do not rely on “cleanup after the fact” as the primary fix
- the primary fix should prevent double-wrapping from happening at all
- any cleanup/normalization should be a defensive backstop only
- keep the fix upstream and general for all generated packages
- use this Studio error and latest package as the grounding evidence
