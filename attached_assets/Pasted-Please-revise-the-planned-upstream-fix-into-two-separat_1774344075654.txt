Please revise the planned upstream fix into two separate tasks.

I agree with the analysis that these should be split:
- one task for XAML Assign argument-wrapper correctness
- one task for dependency version / assembly alignment authority

Please create both tasks in the usual format:
- title / objective
- root-cause summary
- impact analysis
- done looks like
- out of scope
- tasks
- relevant files
- acceptance criteria
- dependencies

Task 1 requirements — XAML Assign Argument Wrapping Idempotence
Important guidance:
- `compliancePass` in `xaml-compliance.ts` should be the authoritative owner of `Assign.To` / `Assign.Value` wrapping
- the quality gate auto-remediation layer in `package-assembler.ts` should not re-wrap those Assign properties
- if it still touches them, it must be truly idempotent and whitespace-tolerant

Also:
- the collapse/de-duplication logic should remain only as a defensive backstop, not the primary fix path
- the primary fix is to prevent double-wrapping from happening at all
- add a post-pass validation that `Assign.To` and `Assign.Value` have exactly one correct wrapper layer

Task 1 should explicitly address:
- nested InArgument / OutArgument accumulation
- whitespace/newline false matches in the quality-gate wrapper logic
- final Studio error `System.Activities.InArgument(System.Object).Expression`

Task 1 acceptance should include a concrete regression test:
- generate the BirthdayGreetings_Autonomous package
- open it in UiPath Studio 25.10+
- verify:
  - no `InArgument.Expression` exception on open
  - no Assign property has more than one wrapper layer

Task 2 requirements — Dependency Version and Assembly Alignment Authority
Important guidance:
- dependency version authority should come from the approved metadata/catalog source of truth
- emitted versions must be validated/restorable
- do not emit non-restorable versions like the current `UiPath.System.Activities 25.10.7` if that is not actually resolvable in the approved environment

Also include:
- alignment between XAML namespace imports / assembly references and `project.json` dependencies
- specifically address the case where `UiPath.Web.Activities` is imported in the XAML but removed from dependencies during alignment
- ensure dependency removal logic does not strip packages still required by assembly references / namespace usage

Task 2 acceptance should include:
- generate the BirthdayGreetings_Autonomous package
- open in UiPath Studio
- verify:
  - dependency restore completes without the current version-resolution errors
  - declared dependencies align with actual XAML imports/assembly references
  - no false-pruned dependency like `UiPath.Web.Activities` remains missing if the XAML still references it

Please also include:
- explicit recommendation that these two tasks can run in parallel
- any dependency between them only if absolutely necessary
