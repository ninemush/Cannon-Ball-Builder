This migration analysis is strong. Please turn it into one concrete implementation task.

Key architectural direction:
MetadataService should become the single authoritative source of truth for UiPath integration endpoint metadata, scope metadata, confidence metadata, and reachability metadata.
We should migrate remaining hardcoded endpoint/scope logic into MetadataService-backed resolution and then retire the hardcoded paths.

Important:
Do not introduce a parallel metadata mechanism.
Do not preserve redundant hardcoded truth longer than necessary.
Do not break existing app flow.

Please create one implementation task with:

1. objective
2. done looks like
3. out of scope
4. tasks
5. relevant files
6. acceptance criteria

The task must include:

A. MetadataService migration completion
- migrate remaining hardcoded endpoint/scope logic to MetadataService-backed resolution
- remove duplicated token logic
- remove redundant fallback scope maps where MetadataService already owns the truth

B. Missing service metadata
- add the missing service types currently used by probes/diagnostics:
  - Integration Service
  - Automation Ops
  - Automation Store
  - Apps
  - Assistant
  - Agent Studio
  - Autopilot
  - reinfer_/IXP alignment
- resolve any service slug mismatches explicitly, not with ad hoc hardcoding

C. Multi-slug service strategy
- explicitly address services like Test Manager and Document Understanding that currently use multiple URL variants
- define how MetadataService should represent:
  - alternates
  - preferred endpoint
  - fallback candidates
  - or resolved active endpoint after probe
- do not oversimplify these into a single-template model if that would lose working behavior

D. Probe and UI truthfulness
- once MetadataService owns endpoint/scope truth, probes should expose:
  - confidence
  - reachability
  - configured/licensed state where appropriate
- UI/status layers must not flatten weak or inferred results into optimistic “available”

E. Flow safety
- preserve current successful flows
- regression-test:
  - connection test
  - diagnostics deep probe
  - service discovery/probe flow
  - deployment/provisioning paths
  - any UI path showing availability or diagnostic status

Important acceptance criteria:
- no duplicated auth/token logic remains for the same UiPath resource path
- endpoint/scope resolution has a single authoritative source
- remaining hardcoded cloud.uipath.com service-root strings in active integration logic are eliminated or explicitly justified as temporary compatibility fallbacks inside MetadataService only
- multi-slug services continue to work after migration
- probe results and UI statuses are more truthful, not more optimistic
- app flow is preserved

Return the full task ready for execution.
