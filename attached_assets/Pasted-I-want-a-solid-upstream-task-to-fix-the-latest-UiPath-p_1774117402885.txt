I want a solid upstream task to fix the latest UiPath package-quality failures without breaking anything else in the app.

Important:
This must be an upstream generator correctness task.
Do NOT patch the generated package manually.
Do NOT introduce regressions in the broader app flow.
Treat current successful app behavior as a compatibility contract unless an intentional correctness improvement is required.

Problem
The latest generated AutoBirthdayMessage package opens in UiPath Studio, but quality is still not acceptable.

What Studio shows:
- UiPath.Web.Activities >= 1.20.1 cannot be restored
- Main.xaml displays ErrorActivity blocks because key activity packages are unresolved
- types like InvokeWorkflowFile and LogMessage are not resolving cleanly in the designer

What the generated files show:
1. Dependency/package graph is still wrong
- project.json still emits package versions that do not restore cleanly in Studio 25.10
- projectProfile is still emitted as "Developement"
- unresolved package graph causes downstream designer activity resolution failures

2. Expression emission is still corrupted
Examples:
- malformed LogMessage.Message expression quoting/bracketing
- broken completion log expression in InitAllSettings.xaml
- expression-bearing attributes are still not being normalized safely enough

3. Type contract issues still remain
Examples:
- GetCredential / SecureString handling still writes into x:String variables in InitAllSettings.xaml
- variable/activity contract validation is still incomplete

4. Process completeness is still weak
Examples:
- HttpClient Endpoint="" still appears in generated workflows
- some generated runtime steps are structurally present but not functionally complete

Goal
Make the package generator produce Studio-resolvable, dependency-clean, expression-safe, type-safe workflows by construction, without breaking existing app flow.

What I want
Please create one concrete upstream implementation task that addresses the following, in priority order:

1. Dependency/package graph correctness
- investigate and fix why UiPath.Web.Activities 1.20.1 is still being emitted if it does not restore in Studio 25.10
- ensure generated project.json only contains restorable package versions for the active Studio target
- verify whether any additional dependency compatibility rules are needed to prevent designer-level ErrorActivity resolution
- fix emitted metadata issues like projectProfile = "Developement"

2. Expression emission correctness
- fix malformed expression emission in Message and similar expression-bearing attributes
- prevent broken quoting/bracketing patterns from reaching final XAML
- add upstream validation so expression corruption is caught before final package output

3. Activity/variable type contract correctness
- fix remaining SecureString / GetCredential output typing issues
- ensure variable declarations and activity output types are checked together upstream
- eliminate cases where password outputs land in x:String variables

4. Process completeness checks
- flag or block empty required endpoint/url values like HttpClient Endpoint=""
- distinguish between structurally openable workflows and functionally complete workflows
- prevent clearly incomplete runtime steps from shipping as if they were production-ready

Non-negotiable flow-safety constraint
This change must NOT break the existing app flow.

Required guardrails:
1. Preserve current successful behavior where possible.
2. Treat current successful app flow as a compatibility contract.
3. Shared helper/refactor work must be drop-in where possible, not an accidental workflow redesign.
4. If any behavior changes intentionally, call it out explicitly.
5. Regression-test the end-to-end flows touched by this task, not just the generator internals.

What I want back
Please return:

1. concise root-cause summary
2. one upstream implementation task with:
- objective
- done looks like
- out of scope
- tasks
- relevant files
- acceptance criteria

3. impact analysis
Please explicitly call out:
- what parts of the generator are affected
- what other app flows could be impacted
- how you will avoid regressions in:
  - package generation
  - package download
  - document generation
  - deployment/provisioning
  - UI status/reporting

Acceptance criteria must include:
- generated project.json contains only package versions that restore in Studio 25.10
- no unresolved-package-driven ErrorActivity issue for core activities like InvokeWorkflowFile and LogMessage
- expression-bearing attributes no longer emit malformed literal/expression hybrids
- GetCredential SecureString outputs match declared variable types
- empty required HttpClient endpoint/url values are detected and surfaced
- app flow remains intact with no regressions in adjacent features

Important:
- This is an upstream fix
- Do not patch only this package
- Do not break anything else in the app
- Prioritize correctness by construction and flow safety together
