Before making any changes, I need a complete 
diagnostic of how progress streaming currently 
works across ALL stages of CannonBall — not just 
package generation. Answer all questions by reading 
the actual code. Do not summarise from memory. 
Show file names and line numbers for everything.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
SECTION 1 — Server side: event emission inventory
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

List every location in the codebase where a 
progress or pipeline event is emitted. For each 
location show:
- File name and line number
- The exact event type/name being emitted
- The exact payload structure
- Which user-facing stage this corresponds to 
  (e.g. SDD generation, PDD generation, process 
  map building, package generation, deployment)

Organise the results by stage so I can see which 
stages have rich event emission and which have 
none or only a single generic event.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
SECTION 2 — Server side: SSE transport layer
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Show me how SSE events are transported from 
emission point to the HTTP response:

1. How many distinct SSE connections exist?
   List each SSE endpoint, its URL, and which 
   stages/events it carries.

2. For each SSE endpoint, show:
   - How the connection is established
   - How events are written to the stream
   - How the connection is closed or times out

3. Is there a shared event bus or emitter that 
   pipeline stages write to, which then forwards 
   to the SSE stream? Or do stages write to the 
   SSE stream directly?

4. When the user triggers an action via the chat 
   interface (e.g. "regenerate uipath package"), 
   does it use the same SSE stream as direct 
   button actions, or a different path? Show 
   the specific code difference.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
SECTION 3 — Client side: event consumption 
inventory
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

List every location in the client codebase where 
SSE or progress events are received and handled. 
For each location show:
- File name and line number
- Which event types/names it listens for
- What UI update it produces when it receives 
  an event
- What fallback or default it shows when no 
  event has been received

Then answer these specific questions:

1. What produces the "Processing..." message 
   that appears during generation? Show the 
   exact code and what triggers it.

2. What produces the stage-specific messages 
   when they do appear? Show the exact code 
   path that renders a stage summary like 
   "Assembling workflow files..."

3. Are there any event types that are emitted 
   server-side but have no corresponding 
   handler client-side? List them.

4. Are there any UI progress states that have 
   a client-side handler but receive no 
   server-side events for certain stages? 
   List them.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
SECTION 4 — Stage coverage matrix
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Produce a matrix showing every user-facing stage 
in CannonBall and its current streaming coverage. 
Use this format:

Stage | Server events emitted | Client handler exists | What user sees
------|----------------------|----------------------|---------------

Stages to include at minimum:
- Idea creation and feasibility
- SDD generation
- PDD generation  
- Process map generation
- Process map approval
- SDD approval
- Package generation (via Generate button)
- Package generation (via chat "regenerate")
- XAML enrichment
- Catalog validation
- Quality gate
- NuGet packaging
- DHG generation
- Deployment to Orchestrator
- Any other stages that exist in the codebase

For the "What user sees" column, be specific — 
"Processing..." or "Generating..." or the actual 
stage message or nothing.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
SECTION 5 — Root cause identification
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Based on sections 1-4, identify every disconnect 
between what the server emits and what the user 
sees. Categorise each gap as one of:

A) Event emitted server-side but never reaches 
   the SSE stream
B) Event in SSE stream but no client handler
C) Client handler exists but renders generic 
   text instead of the event payload
D) Stage has no server-side events at all
E) Chat-triggered path bypasses event emitters 
   that the button path uses
F) Other — explain

For each gap, show the specific file and line 
where the disconnect occurs.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
SECTION 6 — Current "good" example
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Identify the one stage in CannonBall that has 
the best end-to-end streaming coverage right now — 
where server events are emitted, transported, 
received, and rendered as meaningful messages 
to the user. Show me that complete working path 
as a reference implementation that the other 
stages should follow.

If no stage has fully working end-to-end 
streaming, show me the stage that is closest 
to working and explain what is missing.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
IMPORTANT
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Do not fix anything. Do not propose solutions. 
Just show me the complete diagnostic across all 
six sections with exact file names and line 
numbers. I will review the full picture before 
deciding what to fix and in what order.