I want a focused upstream task to add proper Communications Mining authentication support to the UiPath integration.

Context
We have confirmed that Communications Mining (the `reinfer_` API surface under IXP) does not use the same External Application OAuth client_credentials flow as the rest of the UiPath integration.

Instead, Communications Mining uses its own API token generated from:
- UiPath IXP / Communications Mining
- My Account
- API token

We have already confirmed in the product UI that the user can generate this token and has the necessary project permissions.

Current problem
CannonBall currently treats Communications Mining like a normal UiPath OAuth-probed service/capability.
That is incorrect.

The result is:
- Communications Mining is shown as unavailable/authorization-limited even when the user actually has valid access
- the current probe path uses the wrong auth model
- IXP Platform and Communications Mining are being conflated

Goal
Add proper Communications Mining PAT/API-token authentication support to CannonBall, in a clean upstream way, so that:

- the admin/settings area provides a place to configure the Communications Mining API token
- Communications Mining probing and future API calls use that token instead of External App OAuth
- the rest of the UiPath integration continues to work as-is
- if a Communications Mining token is configured, the integration should “just work” for CM-specific endpoints
- if no token is configured, the UI should classify Communications Mining truthfully and tell the user what to do

Important constraints
- Do NOT introduce regression
- Do NOT remove any existing capability
- Keep this upstream and org-agnostic
- Do NOT hack this in as a one-off special case spread across the codebase
- Reuse MetadataService / taxonomy truth where appropriate
- Keep the UI clean and elegant
- Do NOT clutter the settings page
- Treat current working UiPath integration flows as compatibility contracts

What I want from you

Phase 1 — Impact Analysis
Before implementation, analyze and report:

1. what parts of the current codebase assume Communications Mining uses UiPath External App OAuth
2. what parts need to change to support a separate Communications Mining token
3. what UI/settings areas are impacted
4. what probe/status/remediation paths are impacted
5. what regression risks exist for:
- core UiPath connection
- IXP Platform status
- service/capability taxonomy
- settings UI
- diagnostics
- document generation context
- deployment/provisioning

Phase 2 — Communications Mining Auth Model
Implement a proper separate auth model for Communications Mining.

I want:
1. a dedicated configuration field in the UiPath integration/admin settings for:
- Communications Mining API token
This should be clearly labeled and explained as a Communications Mining / IXP API token, not a UiPath External App secret.

2. the token should be stored securely like other secrets/configured credentials

3. Communications Mining auth should use:
- `Authorization: Bearer <communications_mining_api_token>`

4. the rest of the UiPath integration should continue using:
- UiPath External App OAuth
for Orchestrator, TM, DU, DF, AI, etc.

5. IXP Platform and Communications Mining must be treated as related but distinct:
- IXP Platform may still use the existing OAuth-based IXP probe if appropriate
- Communications Mining must use the separate API-token auth path for `reinfer_` API access

Phase 3 — Probe / Status / Taxonomy Updates
Update the Communications Mining handling so that:

1. if a valid Communications Mining API token is configured:
- the Communications Mining probe uses it
- Communications Mining can show as Available if the probe succeeds

2. if no Communications Mining API token is configured:
- Communications Mining should NOT just look like a broken OAuth service
- it should be classified truthfully, something like:
  - Unavailable (Requires Communications Mining API Token)
  - or Unsupported via External App OAuth
- remediation guidance should explain:
  - go to IXP / My Account / API token
  - generate or copy the Communications Mining API token
  - paste it into CannonBall settings

3. IXP Platform and Communications Mining should remain clearly separated in the status model
- do not imply that IXP OAuth access automatically means Communications Mining API access
- do not imply that Communications Mining failure means IXP itself is broken

Phase 4 — Settings UX
Add this to the admin/settings section in a neat way.

Requirements:
- place it in the UiPath integration/admin area
- keep the UX clean and elegant
- do not clutter the main connection setup
- use progressive disclosure or a clearly scoped subsection if needed
- make it obvious that this token is optional unless the user wants Communications Mining API support
- include a short explanation of where to get it in UiPath

Something like:
- Communications Mining API Token
- “Generate this in UiPath IXP > My Account > API token”

Phase 5 — Verification
After implementation, verify:

1. Communications Mining works with the configured API token
2. Communications Mining is classified correctly when no token is configured
3. IXP Platform behavior remains correct
4. core UiPath OAuth-based services remain unaffected
5. current working connection/probe flows remain working
6. no regression in OR deployment, folder loading, or service verification

What I want back
Please return:

1. concise impact analysis
2. implementation plan
3. files/modules changed
4. how Communications Mining auth is now modeled
5. how the settings UI changed
6. what status/remediation behavior changed
7. regression checks performed
8. what I should test immediately

Acceptance criteria
- Communications Mining no longer relies on UiPath External App OAuth for `reinfer_` API access
- a dedicated Communications Mining API token field exists in the UiPath admin/settings integration area
- Communications Mining probe uses the configured API token when present
- when the token is absent, Communications Mining is classified truthfully with clear remediation
- IXP Platform and Communications Mining are not conflated
- no regression to currently working UiPath services or deployment/connectivity flows
- the UX remains neat and uncluttered
