Please produce the next task proposal as a systemic upstream/emission hardening task, grounded in the latest BirthdayGreetingsV12 evidence and in the current codebase behavior.

Important: I do NOT want a broad undeclared-variable auto-fix strategy based on scanner/linter output. That approach remains too risky and is not the main root-cause lane.

What I think the combined evidence now shows
The remaining failures are coming from an incomplete contract across:
1. property intent from the spec / LLM output
2. catalog truth for activities/properties/types
3. deterministic XAML emission
4. workflow identity / infrastructure dedup
5. package/type/framework resolution in the target UiPath environment

Undeclared-variable warnings are still mostly downstream symptoms of those gaps, not the primary root cause.

What I agree with from the latest forensic findings
- there is a real scoped variable declaration gap, especially for structurally-known locals like ForEach iterator variables and similar workflow-scoped intermediates
- there is a real duplicate workflow collision problem between AI-generated workflows and deterministic REFramework infrastructure workflows
- there are real missing required-property failures on Integration Service / GenAI / WebAPI activity families
- InitAllSettings.xaml remains structurally fragile
- deeply nested CType/JSON-navigation expressions are a recurring syntax-risk pattern
- TODO/PLACEHOLDER cleanup is incomplete

What I do NOT want
Do NOT propose:
- scanner-based undeclared-variable auto-declaration across arbitrary identifiers
- defaulting unknown identifiers to x:String
- any new late-stage variable insertion pass driven by noisy identifier extraction
- broad post-lint expression rewriting across arbitrary expressions

Those remain out of scope and unsafe.

How I want Option A / Option B incorporated
1. Option A should now be explicitly included:
- add a single final authoritative normalization stage
- it must run after compliance/analyzer/convergence and before the quality gate / final truth gate
- it should own only the last-mile deterministic normalization classes that are still safe and contract-driven
- once this stage runs, nothing downstream should mutate those normalized fields again

2. Option B should be included only as lightweight guardrails:
- explicit stage-ownership comments/contracts
- tests/assertions preventing stages from re-touching already-normalized expression/literal fields
- idempotency tests for the final normalization stage
- not a large architecture refactor

What I want the next task to focus on
A. Property-intent contract hardening
- String-typed properties must bias ambiguous values toward quoted literals
- prompt-bearing properties like GenAI Prompt must always serialize as string literal intent, never leak into VB expression contexts
- boolean and enum values must be handled as typed literals, not heuristic strings
- REVIEW_REQUIRED placeholders must not become pseudo-variables

B. Deterministic scoped declaration fixes only where structure makes them safe
- fix ForEach iterator declaration/name handling deterministically from the workflow structure
- fix other clearly scoped/generated locals only where the structure makes their declaration authoritative
- do not widen this into arbitrary scanner-based undeclared-variable insertion

C. Infrastructure workflow dedup before assembly
- prevent AI-generated workflows from colliding with deterministic REFramework/infrastructure workflows before tree assembly and registration
- eliminate phantom duplicates like bracketed/quoted infrastructure workflow variants before they enter deferred writes / archive manifests

D. Catalog-required property enforcement before emission
- for the activity families actually emitted by the benchmark flows, ensure required properties are validated and resolved before XAML assembly
- Integration Service / GenAI / WebAPI activities should fail early and clearly if critical required properties are still missing
- do not rely on generic REVIEW_REQUIRED placeholders flowing into emission as if they were valid values

E. InitAllSettings structural stabilization
- fix the multi-repair / cascading structural corruption path so InitAllSettings survives repeated catalog-property syntax corrections
- if needed, batch or stage these repairs through a single well-formed transform rather than sequential string edits that can corrupt nesting

F. Simplify high-risk expression emission
- for deeply nested JSON/CType/navigation expressions, prefer deterministic intermediate assignments or simpler emitted patterns rather than one large fragile expression
- keep this focused on the verified high-risk patterns from the benchmark flows, not a generic expression rewriting system

G. Final authoritative normalization stage
- runs after convergence, before quality gate
- owns only safe final normalization classes such as:
  - boolean child-element InArgument normalization
  - final enum literal normalization
  - final prompt/string literal coercion where property type is authoritative
  - final deterministic String.Format overflow handling / blocking diagnostic
- explicitly exempt these normalized fields from being reprocessed downstream

H. Lightweight stage-ownership guardrails
- document which stages may mutate which content classes
- add tests/assertions that compliance and analyzer do not re-break normalized literal/expression fields
- add idempotency coverage for the final normalization stage

What I want you to output
1. A short correction of the current diagnosis
Explain why the main issue is not “undeclared variables” in isolation, and why broad scanner-based auto-declaration is still unsafe.

2. A revised task proposal
Use this format:
- title
- what & why
- done looks like
- out of scope
- tasks
- relevant files

3. Be explicit about which parts are:
- blocking issues
- warning/noise reduction
- maintenance/guardrail work

Important
- Ground the task in the latest BirthdayGreetingsV12 artifact evidence
- Include Option A explicitly
- Include Option B only as small guardrails, not as a major refactor
- Keep the task systemic and upstream-oriented
- Optimize for the true goal: packages that open cleanly in Studio, with truthful DHG output and mostly real last-mile business work remaining
