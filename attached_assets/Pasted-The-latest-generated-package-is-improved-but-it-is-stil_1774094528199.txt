The latest generated package is improved, but it is still not usable in UiPath Studio. Please fix this as an upstream package-correctness task, not a manual one-off patch.

Observed package review findings

1. Dependency set is still wrong in project.json
Current package declares a mixed dependency set:
- System.Activities = 4.0.0
- UiPath.Excel.Activities = 2.24.3
- UiPath.Mail.Activities = 1.23.1
- UiPath.System.Activities = 25.10.7
- UiPath.Web.Activities = 1.20.1

Studio confirms at least:
- System.Activities 4.0.0 cannot be restored
- UiPath.Web.Activities 1.20.1 cannot be restored

There is also a Studio error:
- “An item with the same key has already been added. Key: Microsoft.Office.Interop.Outlook”

This strongly suggests dependency/version drift and likely transitive package collisions from mixing old package lines with the 25.10 target.

2. Main.xaml is no longer a stub, which is good, but it still contains invalid activity contracts and weak expressions
Examples from the generated Main.xaml:
- GetCredential.Password writes SecureString into variables declared as x:String
- int_WeatherStatusCode appears to be checked without being clearly assigned before use
- Message="str_LogMessage" appears to be emitted as a literal string instead of an expression
- "Exception.Message" appears as a literal string instead of the exception expression

3. InitAllSettings.xaml is still structurally invalid
ForEach is typed as DataRow, but ForEach.Values is emitted as:
- InArgument x:String for dt_Settings.Rows
- InArgument x:String for dt_Constants.Rows

This should never pass upstream typed validation.

What I want
Fix this as a focused upstream generation-quality task.

Goal
Make the generator produce a Studio 25.10-aligned package that:
- restores dependencies cleanly
- avoids package collisions
- emits correct typed activity/property contracts
- does not emit obvious literal-string-vs-expression mistakes
- produces Main.xaml and InitAllSettings.xaml that are structurally valid

Required work

1. Fix deterministic dependency resolution
- Make MetadataService the sole authority for package versions used in generated project.json
- Eliminate mixed old package lines when the target is 25.10
- Ensure project.json only contains dependency versions that are valid for the target Studio profile
- Investigate whether System.Activities should be emitted at all for this package type/target
- Investigate the Outlook DLL collision and identify which package combination is causing it
- Prevent old Excel/Web/Mail package lines from mixing with 25.10 system package lines unless explicitly verified compatible

2. Fix secure credential typing and activity contract emission
- If GetCredential.Password is used, the receiving variable must be typed correctly for SecureString
- Do not emit GetCredential outputs into x:String password variables
- Re-check other activity contracts for typed output/property mismatches

3. Fix typed collection/ForEach emission
- ForEach over DataTable.Rows must emit the correct collection argument type
- Do not emit InArgument x:String for DataRow collections
- Audit the generator/remediation path so this class of mistake is caught before XAML assembly

4. Fix expression emission
- Do not emit bare string literals where VB expressions are intended
Examples to catch:
- Message="str_LogMessage"
- "Exception.Message"
- similar variable/expression quoting mistakes
- Add validation so these are caught pre-emission, not only after XAML is built

5. Re-run generation validation expectations
The goal is not just “Studio opens the project”.
The goal is:
- dependencies restore cleanly in Studio 25.10
- no obvious duplicate-key/package-collision error on open
- Main.xaml is executable structure, not just openable structure
- InitAllSettings.xaml is type-correct

Deliverable
Please return:

1. concise root-cause summary
2. exact implementation task with:
- objective
- done looks like
- out of scope
- tasks
- relevant files

3. explicit acceptance criteria for:
- clean dependency restore in Studio 25.10
- no stale/mixed package lines in project.json
- correct SecureString handling for GetCredential outputs
- correct ForEach/DataRow collection typing
- no literal-string-vs-expression emission errors in generated XAML

Relevant generated files for grounding this diagnosis
- Main.xaml
- InitAllSettings.xaml
- project.json
- entry-points.json

Important
- Treat this as an upstream generator correctness fix
- Do not solve this by manually editing the generated package only
- We want the next generated package to be structurally and dependency-wise correct by construction
