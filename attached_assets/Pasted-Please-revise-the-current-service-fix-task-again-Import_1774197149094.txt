Please revise the current service-fix task again.

Important context
I already know the relevant UiPath resources/scopes are assigned to the External Application for:
- Data Service
- IXP
- Document Understanding
- AI Center

So for the currently failing items, the most likely issue is in CannonBall’s integration logic:
- wrong token family
- wrong scope request
- wrong auth header usage
- wrong endpoint/probe path
- wrong parent/child capability wiring
- wrong custom probe gating logic
- wrong status classification

New critical finding
The failing items are not all probed the same way.

From the code:
- IXP Platform goes through the generic taxonomy loop
- Generative Extraction has a custom probe handler
- Document Understanding has a custom probe handler
- Communications Mining has a custom probe handler
- AI Center has a custom probe handler
- Data Service is handled separately outside the generic loop

This means the task must not treat all failures as one class of problem.
It needs to explicitly distinguish:
- generic taxonomy-loop probe failures
- custom probe handler failures
- special-case probe failures

Current focus
Please revise the task so the primary targets are:

1. Data Service
2. IXP Platform
3. Communications Mining
4. Document Understanding
5. Generative Extraction
6. AI Center

Secondary target:
- Environments, only to determine whether it is truly a provisioning issue or a probe/classification issue

Keep Autopilot out of the main auth-fix scope unless there is concrete evidence of a supported external API path.

What I want from you

1. Classify each target by probe execution path
For each primary target, explicitly state whether it is handled by:
- generic taxonomy loop
- custom probe handler
- special-case probe path

I want this called out in the task itself.

2. Change the default diagnostic assumption
For the six primary targets above, do NOT default to “Unavailable (Service Not Provisioned).”
Investigate in this order:
- probe execution path type
- token family correctness
- scope request correctness
- granted scopes vs requested scopes
- endpoint/base path correctness
- auth header correctness
- probe path correctness
- gating logic correctness
- parent/child capability modeling correctness

Only classify as not provisioned if the evidence actually proves that.

3. For each primary target, require final findings to report:
- probe execution path type (generic loop / custom handler / special case)
- endpoint/path used
- token family used
- scopes requested
- scopes actually granted
- auth headers used
- response received
- whether the current failure is due to:
  - wrong token family
  - insufficient requested scopes
  - wrong endpoint/path
  - wrong header usage
  - wrong custom handler gating
  - unsupported API
  - actual provisioning/licensing issue
  - parent/child modeling issue

4. Fix path-specific bugs upstream
The task must explicitly require:
- fixing generic-loop metadata/config issues where applicable
- fixing custom probe handler logic where applicable
- fixing special-case probe logic where applicable

Examples:
- if IXP Platform is wrong, fix taxonomy/generic-loop inputs
- if Generative Extraction is wrong, fix the custom handler’s token/path/gating logic
- if Communications Mining is wrong, fix its custom handler
- if AI Center is wrong, fix its custom handler
- if Data Service is wrong, fix the separate special-case probe

5. Keep parent/child relationships explicit
Specifically:
- Document Understanding -> Generative Extraction
- IXP Platform -> Communications Mining

If the child fails but the parent works, explain why clearly.
If the child is incorrectly gated on the wrong parent token/family, fix that.

6. Keep this upstream and org-agnostic
No tenant-specific hacks.
No one-off hardcoded scope strings outside MetadataService/OIDC-backed truth.
No special casing for this one app’s exact scope set.

7. Keep it safe
Regression checks must verify that currently working items still work, especially:
- Orchestrator
- Action Center
- Storage Buckets
- Triggers
- Test Manager
- Platform Management
- Maestro
- OR deployment flow
- folder loading
- connection test

What I want back
Please return a revised task with:
1. tightened objective focused on auth/probe correctness for the six primary targets
2. impact analysis
3. explicit probe-path classification per target
4. exact investigation checklist per target
5. revised tasks
6. revised acceptance criteria
7. final reporting requirements

Acceptance criteria
- Data Service, IXP Platform, Communications Mining, Document Understanding, Generative Extraction, and AI Center are all investigated in this wave
- each target is explicitly classified as generic-loop, custom-handler, or special-case
- for each one, the integration determines the real failure cause rather than defaulting to “not provisioned”
- auth/probe/path/gating bugs are fixed upstream where present
- items that are truly available become available
- items that are truly unsupported or not provisioned are classified with evidence
- no regression to currently working services or OR deployment/connectivity flows
- no unnecessary broadening into unrelated systems
