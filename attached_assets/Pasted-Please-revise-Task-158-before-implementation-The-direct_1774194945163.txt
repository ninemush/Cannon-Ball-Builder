Please revise Task #158 before implementation.

The direction is good, but I want this to be an upstream, org-agnostic fix pattern for still-broken UiPath services/capabilities — not a narrow one-off patch and not an overly risky bundle of unrelated changes.

Important constraints
- Do NOT introduce regression
- Do NOT remove any existing capability
- Do NOT hardcode tenant-specific behavior
- Reuse MetadataService / OIDC-backed auth truth where appropriate
- Keep service truth, remediation, and parent/child capability relationships accurate
- Treat currently working package generation and deployment flow as compatibility contracts unless intentionally improved

What I want changed in the task

1. Separate service probe/auth fixes from generator feature work
The current task mixes:
- Data Service / IXP probe and auth fixes
with
- Data Service native activity generation changes

These are different risk classes.

Please either:
- split this into two tasks
or
- make the task explicitly phased:
  Phase A: probe/auth/status correctness
  Phase B: native Data Service activity generation
with Phase B dependent on Phase A being stable

2. Expand from one-off fixes to a repeatable service-fix pattern
Do not treat this as only Data Service + IXP.
Use these as the first concrete targets in a broader still-broken-service fix pattern.

At minimum include investigation/fix coverage for:
- Data Service
- IXP Platform
- Generative Extraction
- Document Understanding
- AI Center
and identify whether any additional currently non-working services should be included now.

For each target service/capability, require:
- endpoint/base path used
- auth resource/token used
- scopes expected
- scopes actually granted
- probe path used
- official/publicly supported API or not
- exact reason for current non-available status
- remediation guidance
- whether package/deployment/generation behavior should change as a result

3. Keep service-truth fixes upstream and org-agnostic
The revised task must explicitly avoid:
- tenant-specific fixes
- one-off hardcoded scope strings
- local endpoint hacks outside MetadataService authority
- patching generated packages instead of generator logic

4. Be evidence-based about IXP / Generative Extraction auth alignment
Do not hardcode the assumption that Generative Extraction should use IXP rather than DU unless that is verified by:
- first-party docs
or
- confirmed runtime behavior with clear evidence

If ambiguous, the task should surface and resolve that ambiguity explicitly.

5. If native Data Service activities remain in scope, tighten acceptance criteria
If Phase B / native Data Service generation stays in scope, require:
- activity catalog entries added correctly
- package dependency correctness verified
- Studio compatibility verified
- no regression for packages that do not use Data Service
- no generic HttpClient TODO fallback for Data Service operations when a native activity exists

6. Add impact analysis
Before implementation, include explicit impact analysis for:
- service probing
- taxonomy/status UI
- remediation guidance
- package generation
- project.json dependencies
- deployment/provisioning
- document generation context

7. Add regression checks
Require verification that:
- currently working services remain working
- OR deployment remains working
- package generation remains working
- no new dependency/package regressions are introduced
- the fixed services now show truthful status with exact reason

What I want back
Please return a revised task with:
1. revised objective
2. phased plan or split-task recommendation
3. impact analysis
4. revised tasks
5. revised acceptance criteria
6. explicit list of target services/capabilities to investigate now
