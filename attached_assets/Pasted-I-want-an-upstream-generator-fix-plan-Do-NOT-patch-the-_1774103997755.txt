I want an upstream generator fix plan. Do NOT patch the generated package manually.

Problem:
The latest UiPath package shows that the generator is still producing output that is not acceptable in Studio 25.10. We now have enough evidence to treat this as an upstream generator correctness problem across three layers:

1. Dependency graph correctness
2. Typed XAML/activity contract correctness
3. Process-logic completeness

I want the fix plan structured around those three layers.

Layer 1 — Dependency Graph Correctness
Observed issues:
- project.json still contains a broken mixed-era dependency set
- System.Activities appears as a package dependency even though it is a framework assembly, not a restorable UiPath/NuGet package
- Studio shows a duplicate-key collision involving Microsoft.Office.Interop.Outlook
- old package lines and 25.10 lines are still being mixed

What I want:
- identify exactly where invalid package IDs and stale package versions are entering the dependency resolver
- make MetadataService the authoritative source for versioned package dependencies
- ensure framework assemblies never reach project.json
- add an upstream compatibility validation step that rejects invalid or conflicting package sets before package emission
- investigate and confirm the actual transitive package combination behind the Outlook collision before encoding any guard rule

Layer 2 — Typed XAML / Activity Contract Correctness
Observed issues:
- GetCredential.Password emits SecureString outputs into variables declared as x:String
- ForEach over DataTable.Rows is emitted with incorrect collection/value typing
- expression-bearing attributes still emit literal strings where VB expressions are intended
- malformed nested InArgument structures are still reaching final XAML

What I want:
- add or strengthen pre-emission validation so activity output types and variable declarations are checked together
- add DataRow/collection typing inference and validation for ForEach
- normalize expression-bearing attributes like Message, Condition, To, Value, etc.
- reject or auto-correct malformed nested argument structures before final XAML assembly

Layer 3 — Process-Logic Completeness
Observed issues:
- some API endpoints are empty
- HTTP status-code variables are used in decisions but are not clearly assigned before use
- some runtime logic is structurally present but incomplete

What I want:
- identify where process-logic placeholders or incomplete specs are still passing through as if they were complete
- add an upstream completeness check that flags missing required endpoint/config values and dead status-code gates before final package emission
- distinguish between “openable structure” and “functionally complete workflow”

Deliverable:
Please return a concrete upstream implementation task with:
1. concise root-cause summary grouped by the 3 layers above
2. objective
3. done looks like
4. out of scope
5. tasks
6. relevant files
7. acceptance criteria

Important:
- This is an upstream fix plan, not a package patch
- Prioritize dependency graph correctness first
- Then typed XAML/activity correctness
- Then process-logic completeness
- The goal is for the next generated package to be correct by construction, not repaired after the fact
