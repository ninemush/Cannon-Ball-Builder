Please reframe the next task around the true upstream/systemic root cause from the latest BirthdayGreetingsV12 run.

I do NOT want the next task centered mainly on undeclared-variable reconciliation. That is a symptom, not the deepest cause.

What I think the latest evidence shows
The remaining failures are coming from an incomplete end-to-end contract between:
1. property intent in the spec / LLM output
2. catalog truth for activities/properties/types
3. deterministic XAML emission
4. package/type/framework resolution in the actual target UiPath environment

The latest run improved materially:
- 44% readiness
- only 1 workflow stubbed
- 9 workflows generated
- manual effort down to ~4.1 hours

But it is still not near the goal because the remaining issues are still system-contract failures, not mostly last-mile business gaps.

What I agree with from the forensic findings
- natural-language GenAI prompt text is leaking into VB expression contexts
- catalog gaps are still real
- package/type/framework truth is still incomplete
- boolean and enum handling still need contract-level fixes
- Init.xaml remains a critical upstream blocker because it is still stubbed

What I do NOT want
Do NOT propose:
- scanner-based undeclared-variable auto-declaration
- defaulting unknown identifiers to x:String
- any new late-stage variable insertion pass
- broad post-lint expression rewriting

Those have already been ruled out as unsafe.

What I want instead
Please produce a revised task proposal centered on these four systemic areas:

A. Property-intent contract hardening
- For prompt-bearing properties like GenAI Prompt, ensure the system treats them as literal/string intent so natural-language text cannot leak into VB expression slots
- Booleans and enums should be handled as typed literals, not heuristic flat strings
- If this connects to ValueIntent or typed property normalization, say so explicitly
- Add catalog-aware property-type coercion in the expression builder:
  - if the catalog says a property expects String, ambiguous values should strongly bias toward literal/string
  - multi-word values should default to string literal unless they clearly match a known VB pattern such as String.Format(...), CType(...), concatenation, etc.
- Call out the REVIEW_REQUIRED / placeholder path explicitly if it bypasses the normal expression-builder safeguards

B. Catalog-as-source-of-truth completion
- Close the remaining catalog gaps for the activities actually emitted by benchmark flows
- Remove any remaining hardcoded required-property or member assumptions that contradict the catalog
- Ensure emission consults catalog metadata for:
  - required properties
  - property types
  - xamlSyntax / attribute vs child-element rules
  - enum valid values
- Assess whether the hardcoded namespace/package maps are now the weak link:
  - if activity/package/type mappings currently live partly in hardcoded maps and partly in the catalog, propose how to consolidate them so the catalog/runtime metadata becomes the primary source of truth
  - hardcoded namespace/package maps should ideally become fallback-only, not the main authority

C. Package/type/framework truth
- Verify emitted activities/types against the actual target UiPath environment and package/profile assumptions
- Explicitly cover the current unresolved activity/type classes:
  - GenAI / Integration Service
  - DataService
  - ComplexScenarios
  - ShouldRetry
  - State / StateMachine-related types
  - MailMessage / mail activity types
- Be explicit whether each issue is:
  - wrong package
  - wrong namespace
  - wrong assembly/type mapping
  - unsupported profile/framework assumption
  - wrong property/member name

D. Safe deterministic emission fixes
- Explicitly include boolean InArgument quote leakage as a contract bug:
  - if a Boolean child-element argument is emitted as "True"/"False" with quotes, fix that at the source in emission
  - a narrow post-emission sanitizer is acceptable only as a fallback safety net, not the primary fix
- String.Format >10 arguments:
  - prevent it in prompts
  - harden parser/fallback logic
  - only rewrite when decomposition is deterministic and safe
  - otherwise emit a precise blocking diagnostic
- Keep any bare-token auto-quoting extremely narrow and type-aware:
  - only when the property’s expected type is String
  - only when the full value is a bare token/path
  - never for multi-token expressions
  - never based on noisy identifier extraction alone

E. Init.xaml as a first-class blocker
- Treat Init.xaml generation failure as a foundational upstream issue, not just another workflow stub
- Propose how to make Init-generation more reliable without broad downstream patching

What I want you to output
1. A short correction of the current diagnosis
Explain why undeclared-variable reconciliation is not the main root-cause lane.

2. A revised task proposal
Use this format:
- title
- what & why
- done looks like
- out of scope
- tasks
- relevant files

3. The task must be systemic, not a symptom patch
It should be grounded in the latest BirthdayGreetingsV12 evidence and clearly distinguish:
- blocking issues
- warning-level noise

Important
- Keep scanner-based variable declaration out of scope
- Keep x:String defaulting out of scope
- Keep late-stage variable insertion out of scope
- Optimize for the true goal: packages that open cleanly in Studio with truthful DHG output and mostly real last-mile work remaining
