Task #90 — Improve Meta-Validation Bar UX

Implement all four tasks in the existing spec 
with the following adjustments. These replace 
the placeholder content in the original spec.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
LABEL
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

The visible label next to the shield icon 
must read "Quality Check" — not "XAML Review". 
The audience includes non-technical Process 
SMEs who may not know what XAML is. Quality 
Check communicates the purpose without 
requiring technical knowledge.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
OVERALL INFO TOOLTIP
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

The info icon tooltip that explains what 
Quality Check is must use this exact copy:

"Quality Check runs an AI-powered review of 
your generated package before it is built. 
It checks for structural errors, missing 
properties, and common XAML mistakes that 
would prevent the package from opening 
correctly in UiPath Studio.

The review uses a lightweight AI model 
(Haiku) to keep costs low."

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
MODE TOOLTIPS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Each mode button must show a tooltip on 
hover using Shadcn Tooltip. Use this exact 
copy for each mode:

AUTO:
"Runs a quality review only when risk 
factors are detected — such as multiple 
workflows, complex API integrations, or a 
low template compliance score. You'll see 
a notice in the progress log when it 
activates.

⏱ +10–30s when triggered
💰 Small token cost when triggered"

ALWAYS:
"Runs a quality review on every generation 
regardless of complexity. Best for critical 
processes where getting it right first time 
matters more than speed. Uses a lightweight 
AI model to keep costs low.

⏱ +10–30s on every generation
💰 Small token cost on every generation"

OFF:
"Skips the quality review entirely. Fastest 
builds with no additional cost. Generated 
package ships without a second-pass check — 
recommended only for simple processes or 
when iterating quickly.

⏱ No additional time
💰 No additional cost"

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
IMPLEMENTATION REQUIREMENTS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1. Use Shadcn Tooltip for mode button tooltips 
   and the info icon tooltip. Keep the bar 
   compact — no permanent visible text beyond 
   the label and toggle.

2. The ⏱ and 💰 lines in the mode tooltips 
   must be visually distinct from the 
   description text — render them in a muted 
   colour or with slightly smaller font size 
   so the cost/time impact reads as a summary 
   row, not body text.

3. Tooltips must work correctly in both light 
   and dark mode. Test both before marking 
   complete.

4. Tooltips must not overflow the sidebar or 
   viewport edge. Use Shadcn Tooltip's side 
   and align props to position them correctly 
   given the bar sits near the top of the 
   chat panel.

5. The info icon must be visually subtle — 
   a small circle-i icon in muted colour, 
   consistent with the existing icon style 
   in the UI. It should not compete visually 
   with the shield icon or the mode toggle.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
COMPLETION CRITERIA
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1. Bar shows "Quality Check" label next to 
   shield icon

2. Info icon is present and shows the overall 
   explanation tooltip on hover

3. Each mode button (Auto, Always, Off) shows 
   its tooltip on hover with the correct copy 
   and ⏱/💰 impact lines

4. Tooltips render correctly in both light 
   and dark mode

5. No tooltip overflows the sidebar or 
   viewport edge in any mode

6. Bar height and layout are unchanged — the 
   additions do not push content down or 
   increase the bar size

7. All existing tests still pass

8. Share a screenshot showing the Auto mode 
   tooltip visible in both light and dark mode