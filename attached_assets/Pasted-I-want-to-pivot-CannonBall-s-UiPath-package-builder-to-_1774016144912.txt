I want to pivot CannonBall’s UiPath package builder to a new architecture.

Source of truth:
Use the current live CB2 codebase.

Goal:
Re-architect the package builder so it reliably produces a UiPath package that:
- opens in UiPath Studio
- can be deployed to UiPath successfully
- degrades gracefully at the finest possible grain
- generates a DHG that captures all completed work, all remaining developer work, and estimated effort

Core architecture direction:
1. Introduce a first-class server-side UiPathGenerationRun model
2. Route all generation entry points through one canonical startUiPathGenerationRun flow
3. Separate transport visibility state from true backend run state
4. Separate spec generation from executable package generation
5. Keep LLM output constrained to structured workflow intent, not raw final XAML ownership
6. Make failure handling finest-grain:
   - property
   - activity
   - sequence
   - workflow
   - package only as last resort
7. Make DHG generation derive from a structured PipelineOutcomeReport with explicit developer effort estimates

What I want from you:
1. Review the current public CB2 implementation
2. Produce a concrete rearchitecture spec for the UiPath package builder
3. Define:
   - run model
   - backend contracts
   - frontend observation model
   - generation phases
   - remediation ladder
   - DHG contract
   - acceptance criteria
4. Preserve the valuable current backend work:
   - activity catalog
   - workflow tree assembly
   - quality gate
   - survivability / fallback
   - outcome reporting
5. Minimize throwaway work, but be honest about which current frontend orchestration pieces should be replaced

Deliverable format:
- problem statement
- target architecture
- run model
- build phases
- remediation model
- DHG requirements
- implementation plan in phases
- explicit acceptance criteria

Important:
- Do not propose more patch-style fixes to the current frontend orchestration as the main answer
- This should be a real architecture spec for the next version of the package builder
- The ambition is near-executable, enterprise-grade output with graceful degradation and truthful UX
