I want an implementation task that turns the #154 UiPath taxonomy/truthfulness design into real code changes, while also investigating and fixing the stubborn services that still are not working correctly.

Important constraints:
- Do NOT introduce regression
- Do NOT remove any existing capability
- Do NOT create redundant metadata/source-of-truth layers
- Lean into MetadataService wherever it makes sense
- Keep the UI truthful, consistent, and intuitive
- Start with impact analysis before finalizing the implementation plan
- Treat current successful app flow as a compatibility contract unless an intentional correction is required

Context
Task #154 was design-only. It did not change runtime behavior or UI behavior.

Current practical problem:
- the top-level UiPath service list is still mixing services, capabilities, and observations
- statuses are still not consistently truthful
- some services I know I have in UiPath still are not showing correctly
- specifically, I want focused investigation for:
  - Integration Service
  - Automation Ops
  - Apps
  - AI Center

I know I have relevant UiPath scopes/resources for these areas in External Applications.
So if they are still showing as unavailable/unknown/limited, I want us to determine exactly why.

What I want from you

Phase 1 — Impact Analysis
Before implementation, analyze and report:

1. Current impact surface
Identify all places affected by taxonomy/status changes and targeted service fixes:
- settings / connection wizard
- top-level service availability UI
- diagnostics / deep probe
- capability discovery
- TO-BE / PDD / SDD platform context
- artifact generation decisions
- deployment/provisioning decisions
- any prompt/context generation that consumes UiPath capabilities

2. Current source-of-truth ownership
Explain what is currently owned by:
- MetadataService
- probe logic
- frontend presentation logic
- any hardcoded taxonomy/status mapping
Call out where MetadataService should become authoritative versus where a thin normalization layer is appropriate.

3. Regression risk analysis
Identify risks to:
- working connection flow
- existing verified services
- deployment/provisioning
- package/artifact generation
- document generation/context
- diagnostics/live ops
and explain how the implementation will avoid regressions.

Phase 2 — Implement the #154 Taxonomy/Truthfulness Model
Implement the design from #154 in actual code.

I want:
1. Canonical resource -> service -> capability mapping
- normalize External App resources into user-facing top-level services
- separate:
  - top-level services/products
  - capabilities/features
  - environment observations/signals
- do not surface raw resource names directly unless that is actually the correct user-facing label

2. MetadataService-backed ownership
- put the canonical taxonomy/status ownership in MetadataService where it makes sense
- do not create a parallel redundant registry
- if a thin normalization layer is needed between MetadataService and the UI, keep it minimal and explain why

3. Truthful status model
Use clear user-facing states.
At minimum support:
- Available
- Unavailable (Endpoint Failure)
- Unavailable (Service Not Available)
- Unavailable (Authorization/Scope)
- Unknown
- Unsupported External API
- Internal Probe Error

Do not use vague “inferred” style user-facing labels.

4. Top-level UI consistency
Make the top level show Service vs Capability consistently.
Examples:
- top-level services should be normalized product/service names
- capabilities like Communications Mining / Generative Extraction / Storage Buckets should not appear as misleading peer services unless explicitly justified
- observations like Attended Robots / Studio Projects should be rendered as observations/signals, not peer services

5. Wizard simplification
Simplify the connection wizard so setup is less dense while preserving all capability.
Do not remove power features.
Move secondary diagnostics/detail out of the primary setup path where appropriate.

Phase 3 — Targeted Service-by-Service Investigation and Fixes
After the taxonomy/status implementation work, investigate and fix these one by one:

1. Integration Service
2. Automation Ops
3. Apps
4. AI Center

For each service, determine:
- official endpoint/base path currently being used
- auth/resource/scopes being used
- whether the endpoint is correct
- whether the probe is correct
- whether the service truly supports external API access in the way we expect
- whether our status/result classification is correct
- whether deployment/use capability is actually supported
- what exact reason explains the current UI state

For each one, fix the real issue:
- endpoint mapping
- auth/resource mapping
- scope handling
- probe logic
- status classification
- UI wording
as appropriate

Important:
Do not “green” a service unless it truly meets the intended standard.
“Available” should mean the service is genuinely confirmed and usable in the way the app needs.

Phase 4 — Deliverable
Please return:

1. concise impact analysis
2. implementation plan
3. files/modules to change
4. service-by-service findings for:
- Integration Service
- Automation Ops
- Apps
- AI Center
5. what code was changed
6. what statuses/taxonomy changed in the UI
7. regression checks performed
8. what I should test immediately

Acceptance criteria
- no regression to currently working UiPath connection flows
- no loss of existing capability
- top-level UI consistently distinguishes services from capabilities/observations
- MetadataService is reused where appropriate instead of bypassed or duplicated
- user-facing statuses are truthful and intuitive
- no vague “inferred available”-style states remain in the UI
- Integration Service is either working correctly or clearly classified with the exact reason why not
- Automation Ops is either working correctly or clearly classified with the exact reason why not
- Apps is either working correctly or clearly classified with the exact reason why not
- AI Center is either working correctly or clearly classified with the exact reason why not
- working services remain working
- downstream design/deployment/generation flows are not broken

Important:
- This is not just a UI labeling task
- This is not just a metadata task
- This is implementation of the taxonomy/truthfulness model plus real service investigation/fixes
- Start with impact analysis, then implement carefully, then validate the four target services one by one
