Please implement the two narrow fixes identified by the required-property fallback runtime trace, and treat them as a sequenced pair with explicit dependency validation between them.

Context
The investigation found two proven loss points:

1. Primary loss point
`LogMessage.Message` fallback never executes because `isGenericTypeDefault("", "System.Object")` returns false. The catalog declares `LogMessage.Message` as `clrType=System.Object`, so the empty string emitted by the deterministic generator is not recognized as a generic default. That prevents the existing `blockOnGenericDefault -> tryContextDerivedFallback` path from running.

2. Secondary loss point
`InvokeWorkflowFile.WorkflowFileName` fallback is successfully resolved by the enforcer, but `injectResolvedPropertyBindings` does not persist it because the attribute already exists with an empty value and the current writeback logic skips replacement.

Goal
Implement both fixes, but do them in the correct sequence and verify the first before relying on the second.

Required execution sequence
Phase 1 first:
Fix `isGenericTypeDefault` for `System.Object`.

Then validate:
- confirm `LogMessage.Message` now reaches the context-derived fallback path
- confirm the fallback is applied in the enforcer output for a real or representative emitted example

Only after Phase 1 is verified should you do Phase 2.

Phase 2 second:
Fix `injectResolvedPropertyBindings` so it replaces existing empty-value attributes when a resolved binding exists.

Then validate:
- confirm `InvokeWorkflowFile.WorkflowFileName` now persists the resolved value into final content instead of remaining `""`

Important
Do not combine both fixes blindly and report only the end result. I need explicit confirmation that:
- Fix 1 addresses the earliest proven loss point for `LogMessage.Message`
- Fix 2 addresses the separate writeback loss for `InvokeWorkflowFile.WorkflowFileName`

Implementation scope
Phase 1
In `server/required-property-enforcer.ts`, update `isGenericTypeDefault` so that for `System.Object` typed properties, empty string `""` is treated as a generic default in the same way as `Nothing` and `null`.

Expected semantic intent:
- this should unblock the existing fallback infrastructure for `LogMessage.Message`
- do not broaden the change beyond what is needed for `System.Object`

Phase 2
In `server/required-property-enforcer.ts`, update `injectResolvedPropertyBindings` so that when:
- a binding has a valid resolved value
- the target attribute already exists
- and the existing value is empty `""`

the function replaces the empty attribute value with the resolved value instead of skipping writeback.

Expected semantic intent:
- this should allow `InvokeWorkflowFile.WorkflowFileName` to persist a context-derived fallback like `AutoGenerated_Invoke_Invoke_Process.xaml`
- do not broaden this into general attribute rewriting beyond the proven empty-attribute case unless necessary

Output requirements
Return results in this exact structure:

1. Phase 1
- files changed
- exact code change summary
- tests run
- runtime or test evidence that `LogMessage.Message` now enters and applies context-derived fallback
- whether it is safe to proceed to Phase 2

2. Phase 2
- files changed
- exact code change summary
- tests run
- runtime or test evidence that `InvokeWorkflowFile.WorkflowFileName` now persists the resolved fallback into final content
- any regressions introduced

3. Final note
- what these two fixes do NOT solve yet
- whether the next remaining issue is more likely generator-side, policy-side, or workflow-assembly-side

Regression expectations
Please add or update focused regression coverage for:
- `isGenericTypeDefault("", "System.Object")`
- `LogMessage.Message` reaching context-derived fallback when emitted as empty string
- `injectResolvedPropertyBindings` replacing an existing empty attribute with a resolved value
- `InvokeWorkflowFile.WorkflowFileName` persisting its fallback into final content

Relevant files
- server/required-property-enforcer.ts
- server/__tests__/required-property-diagnostics.test.ts
- any narrowly relevant test file already covering required-property enforcement or package assembly behavior

Constraints
- keep the changes narrow
- do not implement broader fallback policies in this task
- do not change namespace/dependency logic here
- do not mix in fixes for ReadTextFile.File, SendSmtpMailMessage.Body, or QueryEntity.EntityType yet