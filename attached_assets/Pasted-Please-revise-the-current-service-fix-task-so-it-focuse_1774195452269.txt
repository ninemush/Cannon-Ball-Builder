Please revise the current service-fix task so it focuses specifically on the services that are still failing even though I already have the relevant UiPath resources/scopes assigned to the External Application.

Important context
For these services, assume the most likely problem is NOT “service not provisioned.”
I already know I have the relevant UiPath scopes/resources assigned for:
- Data Service
- IXP
- Document Understanding
- AI Center

So if these are still failing in CannonBall, the likely issue is in our integration layer:
- wrong token family
- wrong scope request
- wrong endpoint/probe path
- wrong auth header usage
- wrong parent/child capability wiring
- wrong status classification

Current focus
Please revise the task so the primary targets are:

1. Data Service
2. IXP Platform
3. Communications Mining
4. Document Understanding
5. Generative Extraction
6. AI Center

Secondary target:
- Environments, only to determine whether it is a true provisioning issue or a probe/classification issue

Keep Autopilot out of the main auth-fix scope unless there is concrete evidence of a real external API path we should be using.

What I want from you

1. Change the default diagnostic assumption
For the six primary targets above, do NOT default to “Unavailable (Service Not Provisioned).”
Instead, investigate in this order:
- token family correctness
- scope request correctness
- granted scopes vs requested scopes
- endpoint/base path correctness
- auth header correctness
- probe path correctness
- parent/child capability modeling correctness
Only classify as not provisioned if the evidence actually proves that.

2. For each primary target, report:
- which UiPath resource/token family we are using
- which scopes we request
- which scopes are actually granted in the JWT
- which endpoint/path we probe
- which auth headers we send
- what response we get
- whether the current failure is due to:
  - wrong token family
  - insufficient requested scopes
  - wrong endpoint/path
  - wrong header usage
  - unsupported API
  - actual provisioning/licensing issue
  - parent/child modeling issue

3. Fix the integration/auth issues upstream
For each of the six primary targets:
- fix token family if wrong
- fix scope selection/request logic if wrong
- fix probe header usage if wrong
- fix endpoint/probe path if wrong
- fix parent/child capability wiring if wrong
- fix status classification if wrong

Important:
Do not just relabel them.
If they are actually accessible with the correct token/path, make them work.

4. Keep parent/child relationships explicit
Specifically:
- Document Understanding -> Generative Extraction
- IXP Platform -> Communications Mining

If the child fails but the parent works, explain why clearly.
If the child is incorrectly using the parent’s or sibling’s token family, fix that.

5. Keep this upstream and org-agnostic
No tenant-specific hacks.
No one-off hardcoded scope strings outside MetadataService/OIDC-backed truth.
No special casing for this one app’s exact scope set.

6. Keep it safe
Regression checks must verify that currently working items still work, especially:
- Orchestrator
- Action Center
- Storage Buckets
- Triggers
- Test Manager
- Platform Management
- Maestro
- OR deployment flow
- folder loading
- connection test

What I want back
Please return a revised task with:
1. tightened objective focused on auth/probe correctness for the six primary targets
2. impact analysis
3. exact investigation checklist per target
4. revised tasks
5. revised acceptance criteria
6. final reporting requirements

Acceptance criteria
- Data Service, IXP Platform, Communications Mining, Document Understanding, Generative Extraction, and AI Center are all investigated in this wave
- for each one, the integration determines the real failure cause rather than defaulting to “not provisioned”
- auth/probe/path bugs are fixed upstream where present
- items that are truly available become available
- items that are truly unsupported or not provisioned are classified with evidence
- no regression to currently working services or OR deployment/connectivity flows
- no unnecessary broadening into unrelated systems
