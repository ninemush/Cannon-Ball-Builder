Two separate issues — address them 
in the right order.

ISSUE 1 — "Invalid space after '<'" 
at 9:22 PM

This generation completed but failed 
at XML validation. Task #107 is the 
fix. Is #107 implemented yet? If yes, 
did it run the new intent pipeline or 
fall back? Show me the log entry that 
indicates which path was taken.

If #107 is not implemented yet, that 
is why the XML error persists. Confirm 
status.

ISSUE 2 — Abort signal disconnected 
from LLM calls (separate task)

This causes hung generations that 
never complete — different symptom 
from the XML error. Create a task 
for this with these three fixes:

1. Pass controller.signal to the LLM 
   call inside the tree enricher. 
   Show me the specific line where 
   the LLM is called without the 
   signal so I can confirm the fix 
   location.

2. Add an overall timeout to the chat 
   route's internal fetch to 
   generate-uipath. The timeout should 
   be generous enough to allow complex 
   processes — 8 minutes minimum. 
   On timeout, send a structured error 
   event to the frontend:
   { error: "Package generation timed 
     out — the process may be too 
     complex. Try breaking it into 
     smaller workflows." }

3. Ensure the frontend recovers 
   cleanly when a timeout or abort 
   error arrives — isGeneratingDocRef 
   must be reset and the UI must show 
   a clear error state, not stay 
   stuck on "Generating UiPath 
   package..." indefinitely.

ISSUE 3 — UiPath error bleeding into 
SDD document card

The error text from the UiPath failure 
is appearing as content inside the 
subsequent SDD document card. This is 
the rendering bug visible in the 
screenshot.

Show me why this is happening before 
creating a fix. Is streamingDocContent 
or streamingMsg being set to the error 
text from the UiPath run before the 
SDD generation clears it?

Address issues in this order:
1. Confirm #107 status
2. Create abort signal task
3. Diagnose issue 3

Do not create tasks for 2 and 3 until 
I have confirmed #107 is complete.