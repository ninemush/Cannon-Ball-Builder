This architectural diagnosis is the first one that feels like it explains the repeated instability coherently.

I agree with the core conclusion:

the package-generation pipeline is unstable because too much semantic correctness is being enforced after XAML emission by mutating stages that compensate for each other. The compliance pass is doing too much semantic surgery, the quality gate/remediation loop is mutating output repeatedly, and the pipeline can create new defects while trying to repair old ones.

I want you to convert this architectural diagnosis into a phased implementation roadmap.

This is NOT a request for another long bug list.
I want a stabilization program for the package-generation architecture.

What I want back
Please return a phased plan with the following structure:

1. Phase 1 — immediate stabilization
- the smallest set of changes that will reduce net self-inflicted breakage quickly
- what can be changed now without a major rewrite
- what metrics should improve immediately

2. Phase 2 — deterministic core strengthening
- what semantic generation responsibilities must move out of the compliance pass and into generation-time emission
- in what order those responsibilities should be moved
- what temporary safeguards remain during the transition

3. Phase 3 — read-only validation transition
- how to move from the current mutating quality-gate/remediation loop toward a validation-first, reporting-first model
- what remediation remains automatic and what stops mutating output
- how to avoid regressions while making that transition

4. Phase 4 — canonical pipeline convergence
- what the final single canonical package-generation pipeline should be
- which current stages remain
- which current stages shrink
- which current stages disappear
- how the final pipeline should behave end-to-end

Questions I want explicitly answered
1. What exact code paths should be simplified first?
2. What should be removed from makeUiPathCompliant first, and what should stay there temporarily?
3. How should the quality gate become read-only in stages rather than all at once?
4. Which auto-remediation actions should remain allowed during the transition, and which should stop immediately?
5. What temporary safety nets do we keep while converging to the final architecture?
6. What run-level metrics should we track every package generation to prove convergence?

Metrics I want used
Please build the roadmap around measurable package-generation convergence metrics, including:
- quality gate re-run count per package
- post-compliance defect introduction rate
- compliance idempotency rate
- cascade amplification ratio
- stub ratio
- DHG accuracy against actual Studio-loadability

Important constraints
- one canonical pipeline is the destination
- no permanent multi-path or dual-pipeline architecture
- no permanent auto-selector
- March 26 should be used only as a benchmark/reference for desired behavior, not as a permanent alternate branch
- prioritize net package usefulness and Studio-loadability over “100% compliant” but hollow packages

What I want the roadmap to distinguish clearly
For every proposed change, tell me:
- why this is Phase 1 vs Phase 2 vs later
- what risk it reduces
- what new risk it might introduce
- what evidence would prove it worked

Please ground this in the real code paths already identified:
- compliance pass
- post-compliance catalog conformance
- quality gate remediation loop
- structural preservation / stubbing hierarchy
- DHG / readiness classification
- and the current generation-time emitters

I want the result to be a real stabilization roadmap we can execute, not just architectural principles.
