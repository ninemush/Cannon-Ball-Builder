I want a focused investigation and task-definition pass for two related issues we just hit during SDD generation:

1. SDD generation failed because the parallel LLM generation stage timed out at 120 seconds
2. the progress indicator is still showing synthetic/fake stage progression instead of truthful backend progress

Important:
Do NOT jump straight to implementation yet.
First, I want a precise investigation of both problems, including how the current progress system is actually wired end-to-end.
We have already done substantial work on progress UX, so I want to understand exactly why the UI is still advancing through fake stages every ~6 seconds.

Context
Observed run:
- SDD generation started
- Platform Capabilities stage completed quickly
- Parallel LLM Generation started
- stage failed exactly at the 120s timeout
- frontend progress was showing rotating step labels like “Writing Technical Overview...” with elapsed time, even though the backend was still in the same long-running LLM stage

This means:
- generation reliability needs investigation
- progress truthfulness needs investigation
- and the latter is especially concerning because we already invested in progress improvements

What I want investigated

Part A — SDD generation timeout / reliability
Please investigate:

1. Exact structure of the SDD generation stage
- what the “LLM Parallel Generation” stage actually does
- what each parallel call receives
- prompt sizes / context sizes
- max token settings
- output expectations
- retry/fallback behavior
- whether both calls share the same timeout budget

2. Timeout policy
- where the 120s timeout is enforced
- whether it is global or stage-specific
- whether both parallel calls are aborted together
- whether the timeout policy is appropriate for complex platform-aware SDDs

3. Root-cause analysis
Determine whether the failure is most likely caused by:
- oversized prompts
- oversized expected outputs
- too much platform context
- too-strict timeout
- poor prompt decomposition between prose and artifacts
- or another issue

4. Recommendation shape
I do NOT want a reflexive “just increase timeout” answer.
I want a recommendation on whether the right fix is:
- prompt/context reduction
- output-budget tuning
- stage-specific timeout adjustment
- better decomposition of prose vs artifacts
- or a combination

Part B — Progress indicator truthfulness
Please investigate the current progress system end-to-end.

1. Wiring map
Map exactly how progress is currently wired for SDD generation:
- what backend events are emitted during document generation
- what run-log/stage data is available
- what SSE events reach the client
- what the workspace/progress components actually render
- where the 6-second rotating stage labels come from
- when the UI uses real stage data vs synthetic fallback text

2. Why fake stages are still appearing
I want a precise explanation of why the UI is still progressing through synthetic stage labels rather than reflecting the real backend stage.
Possible areas to inspect:
- whether document generation uses a different path from UiPath generation
- whether the client only has elapsed time but not stage events
- whether the progress component falls back too aggressively to canned labels
- whether earlier progress UX fixes only applied to UiPath generation and not document generation

3. Available truthful data
Determine what real progress data already exists today for SDD generation:
- run IDs
- stage names
- stage start/end
- retries
- timeout/failure info
- status summaries

I want to know whether the truthful data already exists and the UI is ignoring it, or whether the backend is not emitting it.

4. Recommendation shape
Based on the wiring analysis, recommend the right next fix:
- backend event emission changes
- client-side rendering changes
- document-generation progress alignment with UiPath generation progress model
- removal of synthetic stage progression for SDD/PDD if truthful backend stage data is available

Deliverable
Please return:

1. investigation summary for SDD timeout/reliability
2. investigation summary for progress truthfulness
3. exact wiring map for current progress behavior
4. root cause of why the fake/synthetic stage progression is still visible
5. proposed next task(s), in the usual format:
- title / objective
- root-cause summary
- impact analysis
- done looks like
- out of scope
- tasks
- relevant files
- acceptance criteria
- dependencies

Important constraints
- do not implement yet
- do not give me only a timeout increase
- do not assume the current progress behavior is acceptable
- I want the real wiring and the real root causes first
- if the prior progress work only covered UiPath generation and not SDD/PDD generation, call that out explicitly
