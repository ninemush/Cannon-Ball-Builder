<ui:SendSmtpMailMessage ...attributes only...>
  <ui:SendSmtpMailMessage.Body>
    <InArgument x:TypeArguments="x:String">
      [{{body:expression}}]
    </InArgument>
  </ui:SendSmtpMailMessage.Body>
</ui:SendSmtpMailMessage>
```

And add an explicit rule in Section 1:
```
CRITICAL: Body on SendSmtpMailMessage is a 
child element, NOT an attribute. Never write 
Body="..." as an attribute. Always use the 
child element pattern shown in the template.
```

**Fix 3 (cascade stub fallback) — NO, this is code only**

The cascade is in the quality gate's escalation logic, not in anything the LLM produces. The LLM doesn't decide which workflows get stubbed — the pipeline code does. No prompt change can fix this. It's a code fix and a relatively simple one — the escalation logic just needs to scope stubs to the affected workflow only.

---

Here's the combined prompt update to send Replit:
```
Two of the three fixes from the previous task 
should be implemented as prompt changes, not 
code patches. The third (cascade stub fallback) 
remains a code fix.

PROMPT FIX 1 — Level enum violations

In the XAML generation prompt Section 1 rules, 
add this immediately after the existing enum 
constraint rule:

"CRITICAL — LogMessage Level values:
The Level attribute on ui:LogMessage accepts 
ONLY these exact values:
  Trace, Info, Warn, Error, Fatal

The following values look correct but are WRONG 
for UiPath and will cause the package to be 
rejected by Studio:
  Information ✗  — use Info instead
  Warning ✗      — use Warn instead
  Debug ✗        — use Trace instead
  Critical ✗     — use Fatal instead

These wrong values come from .NET's LogLevel 
enum. UiPath uses different names. Always use 
the UiPath values listed above. Never use the 
.NET names."

PROMPT FIX 2 — SendSmtpMailMessage Body 
must be a child element

Update the SendSmtpMailMessage template in 
xaml-template-builder.ts to show Body as a 
child element, not an attribute:

Current template (wrong):
  <ui:SendSmtpMailMessage
    ...
    Body="[{{body:expression}}]"
    ... />

Replace with:
  <ui:SendSmtpMailMessage
    DisplayName="{{displayName:string}}"
    To="[{{to:expression}}]"
    From="[{{from:expression}}]"
    Subject="[{{subject:expression}}]"
    IsBodyHtml="{{isHtml:bool}}"
    SmtpServer="{{smtpServer:string}}"
    Port="{{port:string}}"
    EnableSsl="{{ssl:bool}}"
    Username="[{{username:expression}}]"
    Password="[{{password:expression}}]"
    sap2010:WorkflowViewState.IdRef="{{id:uniqueid}}"
    sap:VirtualizedContainerService.HintSize="334,90">
    <ui:SendSmtpMailMessage.Body>
      <InArgument x:TypeArguments="x:String">
        [{{body:expression}}]
      </InArgument>
    </ui:SendSmtpMailMessage.Body>
  </ui:SendSmtpMailMessage>

Also add this rule to Section 1 of the 
generation prompt:

"CRITICAL — SendSmtpMailMessage Body:
Body is a child element, never an attribute. 
The template shows the correct pattern. 
Never write Body="..." as an XML attribute 
on SendSmtpMailMessage."

CODE FIX — Cascade stub fallback

This cannot be fixed in the prompt — it is 
in the quality gate escalation logic.

Change the stub escalation so that:
1. Per-workflow stubs apply only to the 
   specific workflow with blocking errors
2. Full-package stub escalation is removed — 
   replace with PackageStatus.FAILED that 
   surfaces errors without destroying clean 
   workflows
3. Workflows that pass validation are never 
   stubbed regardless of sibling workflow status

These three changes together — two in the 
prompt, one in code — eliminate the three 
blocking issues identified in the analysis. 
The prompt fixes prevent the errors from 
being generated. The code fix prevents 
collateral damage when any error does slip 
through.

After all three are applied, regenerate 
WeatherAndTravelNotification and send the 
package for inspection.